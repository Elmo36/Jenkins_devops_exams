apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkex.fullname" . }}
  labels:
    {{- include "jenkex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkex.selectorLabels" . | nindent 4 }}
